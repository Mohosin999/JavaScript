/**
 * There is two types of data types in JS
 * 1. Primitive Data Type
 *   - Number
 *   - String
 *   - Boolean
 *   - Undefine (no data is define)
 *   - null (there is no data)
 *
 * 2. Object Data Type
 *   - Array
 *   - Object
 *   - Function
 */

// ***** Number *****
const n = "20";
const m = Number("10"); // 10
console.log(Number.parseInt(n)); // 20, parseInt = return number from string

// Hexadecimal Number
// assign 0x / 0X before write hex num
const hex = 0xff;
console.log(hex);

// Octal Number
// assign 0 before write oct num
const oct = 0756;
console.log(oct);
